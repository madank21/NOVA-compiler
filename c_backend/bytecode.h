#ifndef NOVA_BYTECODE_H
#define NOVA_BYTECODE_H

/* Superseded: the native backend data structures now live in compile.h,
 * the single source of truth. This header is kept as an alias. */
#include "compile.h"

#endif
